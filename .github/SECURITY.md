# Security policy

Please report vulnerabilities privately instead of opening a public issue. Include the affected version, deployment topology, reproduction steps, and expected impact.

Do not include live relay credentials, provider API keys, registry contents, or private network addresses in a report. Revoke any credential that may have been exposed during testing.

Security fixes are prioritized for the current main branch. Older snapshots may be asked to upgrade before a fix is evaluated.
